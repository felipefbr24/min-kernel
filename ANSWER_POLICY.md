# Guidance for responses

Per the latest user request, avoid further modifications to the explanatory files (`explicacion.txt`, `slides.md`, `detalle_tecnico.txt`, `informe.txt`) and keep future interactions focused on answering questions directly rather than editing those documents.
